# browser router monitor

## intsall

  ```
    npm i browser-router-observer -S
  ```

## usage
  
``` javascript
  import bro from 'browser-router-observer';
  bro(console.log(newUrl, oldUrl));
```
